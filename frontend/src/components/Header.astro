---
import votingIcon from '../assets/voting.png';
import { Image } from 'astro:assets';
---

<header>
  <div class="header-content">
    <Image
      src={votingIcon}
      alt="Icono de votación"
      width={60}
      height={60}
      class="voting-icon"
    />
    <div class="header-text">
      <h1>Sistema de Asignación de Escaños D'Hondt</h1>
      <p>Calcula la distribución proporcional de escaños usando el método D'Hondt</p>
    </div>
  </div>
</header>

<style>
  header {
    background: linear-gradient(135deg, #66cdea 0%, #4b8ba2 100%);
    color: white;
    padding: 30px;
  }

  .header-content {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 20px;
  }

  .voting-icon {
    filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.2));
  }

  .header-text {
    text-align: left;
  }

  .header-text h1 {
    font-size: 2.5rem;
    margin-bottom: 10px;
  }

  .header-text p {
    font-size: 1.1rem;
    opacity: 0.9;
  }

  @media (max-width: 768px) {
    .header-content {
      flex-direction: column;
      text-align: center;
    }

    .header-text {
      text-align: center;
    }

    .header-text h1 {
      font-size: 1.8rem;
    }
  }
</style>
